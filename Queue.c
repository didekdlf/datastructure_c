#include <stdio.h>
#include<stdlib.h>









void Ch_3_2() {
	//Queue
	//array list로
	


	//linked list로

}

int main(void)
{
	Ch_3_2();


	/*
	int num[5] = { 10,20,30,40,50 };
	
	int* p= NULL;
	p = (int*)malloc(5*sizeof(int));
	
	int** q = NULL;

	for (int i = 0; i < 5; i++)
		p[i] = i;
	for (int i = 0; i < 5; i++)
		printf("%d",p[i]);

	q = (int**)malloc(10 * sizeof(int*));
	for (int i = 0; i < 10; i++)
		q[i] = (int*)malloc(5 * sizeof(int));
	for (int i = 0; i < 10; i++)
	{
		printf("\n");
		for (int j = 0; j < 5; j++)
		{
			q[i][j] = i + j;
			printf("%4d ", q[i][j]);
		}
		
	}
	int* q;
	for (int i = 0; i < 5; i++) {
		q = &num[i];
		printf("%d\n", int(q));

	}
	printf("\n");
	p = num;
	for (int i = 0; i < 5; i++) {
		printf("%d\n", int(p + i));

	}
	printf("\n");
	p++;
	for (int i = 0; i < 5; i++) {
		printf("%d\n", p + i);
	}
	

	free(p);
	*/
	return 0;

}
